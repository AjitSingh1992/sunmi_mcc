<resources>

    <!--
    Startup application theme.
    Only used on the Manifest <application> node, not on any Activities themselves
    -->
    <style name="FullscreenThemeWithSplash" parent="FullscreenTheme">
        <item name="android:windowBackground">@drawable/native_splash2</item>
    </style>

    <!--
    Main application theme.
    Apply this to every Activity in the manifest as the root <application> node has a different theme (with splash)
    -->
    <style name="FullscreenTheme" parent="NoActionBar">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
